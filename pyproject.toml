[project]
name = "deep-work-bot"
version = "0.1.0"
description = "Telegram bot for sharing deep work availability status."
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.0.1",
    "python-telegram-bot>=22.7,<23",
]
